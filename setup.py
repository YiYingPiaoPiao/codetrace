# -*- coding: utf-8 -*-
"""Setup Configuration for CodeTrace Package

This file configures the package for distribution via PyPI and other package repositories.
It defines package metadata, dependencies, and entry points.
"""

from setuptools import setup, find_packages

setup(
    name="codetrrace"
)